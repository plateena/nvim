;; extend existing capture groups so that the link destination is concealed
;; the `extends` directive ensures this merges with builtin queries
;; this will hide the URL in `[label](url)`
;; check Tree‑sitter docs for query structure
;; make sure this file loads after default queries
